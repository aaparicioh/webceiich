<%= form_with(model: medio, local: true) do |form| %>
  <% if medio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medio.errors.count, "error") %> prohibited this medio from being saved:</h2>

      <ul>
      <% medio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
